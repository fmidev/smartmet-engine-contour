quiet = true;
defaultlogging = false;

engines:
{
	querydata:
	{
		configfile = "querydata.conf";
	};
	
	contour:
	{
		libfile = "../../contour.so";
		configfile = "contour.conf";
	};
};

plugins:
{
};
